package cr.marin.memoriahub.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale; centralized so screens share one Typography instance.
val AppTypography = Typography()
